[project]
name=server

[source]
root=.
recursive=true
extensions=.cpp,.c,.h,.hpp

[target]
output=build/server

[compiler]
command=g++
standard=c++20
flags=-pthread

[include]
paths=.,include

[exclude]
dirs=.git,build,node_modules

[build]
debug=true
optimization=2
warnings=true
clean=true